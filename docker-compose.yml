version: '3'

services:
  mongo:
    image: mongo:latest
    container_name: mongo-db
    restart: always
    expose:
      - 27017
  rune:
    container_name: rune
    restart: always
    command: -update
    build: .
    depends_on:
      - mongo
    volumes:
      - ./data:/data/db